import { defineConfig } from "vite";

export default defineConfig({
  // project related config
  plugins: [
    // plugins
  ],
  server: {
    host: true,
    port: 8181, // change port number if you prefer
  },
});